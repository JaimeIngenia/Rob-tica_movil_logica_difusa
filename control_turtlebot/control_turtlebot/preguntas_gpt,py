EStoy creando un paquete en ROS2, y tengo este error de compilaciòn " AttributeError: 'laneControl' object has no attribute '_scan' ", ti en mi arhivo principal tengo estas importaciones " import cv2
from cv2 import waitKey
import numpy as np
import matplotlib.pyplot as plt
from typing import final
import rclpy
from rclpy.node import Node
from rclpy.qos import QoSProfile
from sensor_msgs.msg import Image
from geometry_msgs.msg import Twist
from cv_bridge import CvBridge, CvBridgeError
from sensor_msgs.msg import LaserScan
from rclpy.qos import qos_profile_sensor_data
from nav_msgs.msg import Odometry
import math
" y el el archivo package.xml tiene estas dependencias "
  <depend> rclpy</depend>
  <depend> geometry_msgs</depend>
  <!-- <depend> turtlesim</depend> -->
  <depend> sensor_msgs</depend>
  <depend> cv2</depend>
  <depend> cv_bridge</depend>
  <depend> numpy </depend>
  <depend> nav_msgs </depend>
  <depend> math </depend>
  <depend> matplotlib </depend>

  <depend> typing </depend>"